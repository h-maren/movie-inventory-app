<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
</head>
<header>
    <%= title %>
</header>
<body>
    <form method="POST" action="/addOrEditMovie">
        <label for="movie_title">Title: </label>
        <input type="text" id="movie_title" name="movie_title" value="<%=movie ? movie.movie_title : ''%>">
        <label for="movie_director">Director: </label>
        <input type="text" id="movie_director" name="movie_director" value="<%=movie ? movie.movie_director : ''%>">
        <label for="movie_year">Year: </label>
        <input type="number" step="1" id="movie_year" name="movie_year" value="<%=movie ? movie.movie_year : ''%>">
        <% categories.map((category) => { %>
            <input type="checkbox" id="<%= category.id %>" name="category[]" value="<%=category.category_type%>" 
            <% if(moviescategories){ %>
            <% moviescategories.map((moviecategory) => {%>
            <% if(moviecategory.categoryid==category.id) {%> checked > <% } else {%> <% } %>
            <% }) %>
            <% } %>
            <label for="<%= category.id %>"><%=category.category_type%></label>
        <% }) %>
        <button type="submit"><%=title%></button>
     </form>
     <form method="POST" action="/deleteMovie/<%= movie.id %>">
        <button type="submit">Delete This Movie</button>
     </form>
     <a href="/">Change Your Mind? Go Back Home</a>
</body>
</html>